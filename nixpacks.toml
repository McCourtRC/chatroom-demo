[phases.setup]
nixPkgs = ['...', 'protobuf', 'tree']

[phases.build]
cmds = ["cargo build --release --bin chatroom-server", "tree", "cp ./target/release/chatroom-server ./app/target/release", "tree"]

[start]
cmd = "./target/release/chatroom-server"
