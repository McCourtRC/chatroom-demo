[phases.setup]
nixPkgs = ['...', 'protobuf', 'tree']

[phases.build]
cmds = ["cargo build --release --bin chatroom-server", "pwd", "tree -L 3", "cp ./target/release/chatroom-server ./app/target/release", "tree -L 3"]

[start]
cmd = "./target/release/chatroom-server"
